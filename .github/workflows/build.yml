name: Build TB (Avalonia Phase 1)
on: [push, workflow_dispatch]
env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: false

      - uses: actions/setup-dotnet@v5
        with: { dotnet-version: '10.0.x' }

      - name: Clean & Restore
        run: |
          rm -rf obj bin publish
          dotnet restore

      - name: Build
        run: dotnet build TB.csproj --configuration Release --no-restore

      - name: Publish
        run: |
          dotnet publish TB.csproj -c Release --self-contained false
          mkdir -p publish
          cp -r bin/Release/net10.0/* publish/
          find publish -name "*.zip" -delete

      - name: Create ZIP
        run: |
          cd publish
          zip -r ../TB-Portable.zip .
          cd ..

      - uses: actions/upload-artifact@v7
        with:
          name: TB-Portable
          path: TB-Portable.zip
          retention-days: 14
