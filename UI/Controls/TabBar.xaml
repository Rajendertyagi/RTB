<UserControl x:Class="TB_Browser.UI.Controls.TabBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource SurfaceBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Tabs (Horizontal) -->
        <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <StackPanel x:Name="TabsPanel" Orientation="Horizontal" Margin="8,0,0,0"/>
        </ScrollViewer>

        <!-- New Tab + Window Controls -->
        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Button Content="+" Width="40" Height="40" Background="Transparent" Foreground="White"
                    BorderThickness="0" FontSize="18" Click="NewTab_Click"/>
            <Button Content="─" Width="40" Height="40" Background="Transparent" Foreground="White"
                    BorderThickness="0" FontSize="16" Click="Minimize_Click"/>
            <Button Content="□" Width="40" Height="40" Background="Transparent" Foreground="White"
                    BorderThickness="0" FontSize="12" Click="Maximize_Click"/>
            <Button Content="✕" Width="40" Height="40" Background="Transparent" Foreground="White"
                    BorderThickness="0" FontSize="12" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
