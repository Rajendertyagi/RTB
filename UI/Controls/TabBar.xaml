<UserControl x:Class="TB_Browser.UI.Controls.TabBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#292a2d">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Hidden">
            <StackPanel x:Name="TabsPanel" Orientation="Horizontal" Margin="8,4,0,4"/>
        </ScrollViewer>
        
        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0">
            <Button Content="─" Width="44" Background="Transparent" Foreground="#fff" BorderThickness="0" Click="Minimize_Click"/>
            <Button Content="□" Width="44" Background="Transparent" Foreground="#fff" BorderThickness="0" Click="Maximize_Click"/>
            <Button Content="✕" Width="44" Background="Transparent" Foreground="#fff" BorderThickness="0" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
