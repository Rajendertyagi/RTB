<UserControl x:Class="TB_Browser.UI.Controls.TabBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource ChromeNavBg}">
    <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden">
                <StackPanel x:Name="TabsPanel" Orientation="Horizontal" Margin="8,0,0,0"/>
            </ScrollViewer>
            <Button Grid.Column="1" Content="+" Style="{StaticResource ChromeNewTabBtn}" Click="NewTab_Click" Margin="4,0,0,0" ToolTip="New tab (Ctrl+T)"/>
        </Grid>
        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,8,0">
            <Button Content="─" Style="{StaticResource ChromeNavBtn}" Click="Minimize_Click" ToolTip="Minimize"/>
            <Button Content="□" Style="{StaticResource ChromeNavBtn}" Click="Maximize_Click" ToolTip="Maximize/Restore"/>
            <Button Content="✕" Style="{StaticResource ChromeNavBtn}" Click="Close_Click" ToolTip="Close"/>
        </StackPanel>
    </Grid>
</UserControl>
