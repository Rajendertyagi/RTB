<Window x:Class="TB_Browser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="TB Browser" Width="900" Height="700"
        WindowStyle="None" ResizeMode="CanResize" Background="#202124"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <WindowChrome.WindowChrome>
        <WindowChrome 
            ResizeBorderThickness="8" 
            CaptionHeight="40" 
            GlassFrameThickness="-1" 
            UseAeroCaptionButtons="False"
            CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.InputBindings>
        <KeyBinding Key="T" Modifiers="Ctrl" Command="ApplicationCommands.New"/>
        <KeyBinding Key="W" Modifiers="Ctrl" Command="ApplicationCommands.Close"/>
        <KeyBinding Key="L" Modifiers="Ctrl" Command="ApplicationCommands.Properties"/>
        <KeyBinding Key="F5" Command="NavigationCommands.Refresh"/>
        <KeyBinding Key="Left" Modifiers="Alt" Command="NavigationCommands.BrowseBack"/>
        <KeyBinding Key="Right" Modifiers="Alt" Command="NavigationCommands.BrowseForward"/>
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ContentControl x:Name="TabBarHost" Grid.Row="0"/>
        <ContentControl x:Name="AddressBarHost" Grid.Row="1"/>
        <ContentControl x:Name="BrowserHost" Grid.Row="2"/>
        <ProgressBar x:Name="LoadProgress" Grid.Row="2" Height="2" VerticalAlignment="Top" 
                     Background="Transparent" Foreground="{StaticResource AccentBlue}" Visibility="Collapsed"/>
        <TextBlock x:Name="StatusText" Grid.Row="3" Margin="8,0" Foreground="#9aa0a6" FontSize="11" Height="20" VerticalAlignment="Center"/>
    </Grid>
</Window>
