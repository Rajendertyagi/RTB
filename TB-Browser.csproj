<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <!-- Supported TFM for WinUI 3 C# apps -->
    <TargetFramework>net10.0-windows10.0.19041.0</TargetFramework>
    <TargetPlatformMinVersion>10.0.17763.0</TargetPlatformMinVersion>
    
    <!-- CI targeting: allows builds without full Windows SDK installed -->
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
    
    <!-- WinUI 3 Unpackaged Configuration (CRITICAL) -->
    <UseWinUI>true</UseWinUI>
    <WindowsPackageType>None</WindowsPackageType>
    <!-- REMOVED: EnableMsixTooling (causes XamlCompiler target conflicts) -->
    
    <!-- XAML Compiler CI Workaround (Per Microsoft Troubleshooting Docs) -->
    <DisableXamlCompilerBinarySerialization>true</DisableXamlCompilerBinarySerialization>
    <GenerateLibraryLayout>false</GenerateLibraryLayout>
    
    <RootNamespace>TB_Browser</RootNamespace>
    <AssemblyName>TB-Browser</AssemblyName>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <Platforms>x64</Platforms>
    <RuntimeIdentifiers>win-x64</RuntimeIdentifiers>
    
    <Nullable>enable</Nullable>
    <LangVersion>14</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    
    <SelfContained>false</SelfContained>
    <PublishSingleFile>false</PublishSingleFile>
    <PublishTrimmed>false</PublishTrimmed>
  </PropertyGroup>

  <ItemGroup>
    <!-- WinUI 3 runtime (manages its own SDK references) -->
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.6.241114003" />
    
    <!-- Data & MVVM -->
    <PackageReference Include="Dapper" Version="2.1.35" />
    <PackageReference Include="Microsoft.Data.Sqlite" Version="8.0.10" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.3.2" />
    
    <!-- Manifest -->
    <Manifest Include="$(ApplicationManifest)" />
  </ItemGroup>

  <!-- XAML: Update only (SDK auto-includes Pages) -->
  <ItemGroup>
    <Page Update="**\*.xaml">
      <Generator>MSBuild:Compile</Generator>
      <SubType>Designer</SubType>
    </Page>
  </ItemGroup>
</Project>
